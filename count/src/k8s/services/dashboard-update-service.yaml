apiVersion: v1
kind: Service
metadata:
  name: dashboard-update-service
  namespace: count-system
  labels:
    app: dashboard-update-service
spec:
  type: ClusterIP
  ports:
    - port: 3005
      targetPort: 3005
      protocol: TCP
      name: http
  selector:
    app: dashboard-update-service
