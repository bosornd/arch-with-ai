apiVersion: v1
kind: Service
metadata:
  name: dashboard-provision-service
  namespace: count-system
  labels:
    app: dashboard-provision-service
spec:
  type: ClusterIP
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
      name: http
  selector:
    app: dashboard-provision-service
