apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: count-system
type: Opaque
stringData:
  # PostgreSQL 비밀번호
  POSTGRES_PASSWORD: "changeme"
  
  # Redis 비밀번호 (필요한 경우)
  REDIS_PASSWORD: ""
  
  # API 키 (실제 운영 환경에서는 적절한 값으로 변경)
  API_KEY: "changeme"
