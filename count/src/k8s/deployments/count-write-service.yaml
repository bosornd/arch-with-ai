apiVersion: apps/v1
kind: Deployment
metadata:
  name: count-write-service
  namespace: count-system
  labels:
    app: count-write-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: count-write-service
  template:
    metadata:
      labels:
        app: count-write-service
    spec:
      containers:
      - name: count-write-service
        image: agentk.count/count-write-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3000
          name: http
        env:
        - name: PORT
          value: "3000"
        - name: POSTGRES_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: POSTGRES_HOST
        - name: POSTGRES_PORT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: POSTGRES_PORT
        - name: COUNT_INFO_DB_NAME
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: COUNT_INFO_DB_NAME
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: POSTGRES_PASSWORD
        - name: REDIS_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: REDIS_HOST
        - name: REDIS_PORT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: REDIS_PORT
        - name: REDIS_CACHE_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: REDIS_CACHE_HOST
        - name: REDIS_CACHE_PORT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: REDIS_CACHE_PORT
        - name: KAFKA_BROKERS
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: KAFKA_BROKERS
        - name: KAFKA_TOPIC_COUNT_UPDATES
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: KAFKA_TOPIC_COUNT_UPDATES
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
